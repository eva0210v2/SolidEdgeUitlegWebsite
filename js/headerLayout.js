// --- headerLayout.js ---
// Geen dynamische aanpassing meer nodig.
// Header en navigatie zijn nu vast met CSS geregeld.

window.addEventListener("load", () => {
  console.log("headerLayout.js: statische layout actief, geen aanpassingen nodig.");
});
